package forestml.refimpl

object Margin {
  /*
  def stripMargin(singleLineInput: StringSlice, delimChar: Char)
  : (StringSlice, StringSlice) = {
    var i = 0
    while (
  }
  */

  /*
  def stripMargin(
    lines: List[StringSlice],
    delimChar: Char
  ): (List[StringSlice], List[StringSlice]) = {
    lines.map(stripMargin).unzip
  }
  */
}